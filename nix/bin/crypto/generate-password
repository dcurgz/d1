#!/usr/bin/env bash
LENGTH="${1:-10}"
ARGS="$@"
if [[ $ARGS == *"-sane" ]]; then
tr -dc 'A-Za-z0-9' </dev/urandom | head -c "$LENGTH"; echo
else
tr -dc 'A-Za-z0-9!"#$%&'\''()*+,-./;:<=>?@[\]^_`{|}~' </dev/urandom | head -c "$LENGTH"; echo
fi
